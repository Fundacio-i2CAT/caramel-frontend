(function (window) {
    window.__env = window.__env || {};
  
    // API url
    window.__env.apiUrl = 'ws://84.88.36.126:8655';
  
    // Whether or not to enable debug mode
    // Setting this to false will disable console output
    window.__env.enableDebug = true;
  }(this));
  